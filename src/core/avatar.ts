import * as THREE from 'three'

export default class Block {
    constructor(scene: THREE.Scene) {
        this.scene = scene
        this.initAvatar()
    }

    scene: THREE.Scene;

    initAvatar = () => {
        
    }
}